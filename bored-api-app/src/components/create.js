import ActivityItem from "./ActivityItem";

const ActivityList = ({activities}) => {    //activities is the data being passed down from app.js
    return (  
        <>
        
        </>
    );
}

export default ActivityList